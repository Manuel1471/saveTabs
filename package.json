{
  "name": "save-tabs",
  "version": "1.2.1",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check popup.js && node --check background.js && node --check options.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8'))\""
  }
}
